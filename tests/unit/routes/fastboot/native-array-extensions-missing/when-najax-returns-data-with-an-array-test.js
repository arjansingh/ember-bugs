import { moduleFor, test } from 'ember-qunit';

moduleFor('route:fastboot/native-array-extensions-missing/when-najax-returns-data-with-an-array', 'Unit | Route | fastboot/native array extensions missing/when najax returns data with an array', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
